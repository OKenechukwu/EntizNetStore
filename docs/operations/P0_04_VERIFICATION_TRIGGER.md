# P0-04 hosted verification trigger

This file exists only on the temporary `p0-04-verification` Git branch to create an isolated Vercel/Supabase preview for the deployed authenticated authorization release gate.

Do not merge this branch. Close the temporary PR and delete this branch after P0-04 evidence is captured.

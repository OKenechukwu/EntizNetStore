name: Migrate Legacy Runtime Callers

on:
  push:
    branches:
      - codex/m0-production-foundation
    paths:
      - scripts/migrate-legacy-supabase-auth-helper.mjs
      - scripts/remove-legacy-dynamic-translation.mjs
      - .github/workflows/migrate-legacy-auth-helper.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Migrate legacy Supabase auth-helper callers
        run: node scripts/migrate-legacy-supabase-auth-helper.mjs

      - name: Remove legacy dynamic translation callers
        run: node scripts/remove-legacy-dynamic-translation.mjs

      - name: Commit migrated callers
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No legacy runtime callers remain"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app components lib client server
          git commit -m "refactor: migrate legacy runtime callers"
          git push
